<other_context>
{{ transcript }}
</other_context>

<user_instructions>
请根据上面的飞书妙记文字记录写会议总结。文字记录可能有转写错误；只修正常见术语误识别。证据不足时，保留原意并标注「不确定」。

## 会议信息

- 标题: {{ meeting.title or "未知" }}
- minute_token: {{ minute.get("minute_token", "") }}
{% set transcript_path = minute.get("rel_transcript_path") or minute.get("source_transcript_path") or "" %}
{% if transcript_path %}- 文字记录路径: {{ transcript_path }}{% endif %}

## 输出格式

输出 Markdown。按这个顺序写：

# {{ meeting.title or "会议总结" }}

## 元数据

- 标题
- minute_token
- 文字记录路径；如果没有路径就省略

## 总览

用一段话说明会议讨论的核心问题、已经形成的结论、仍未确定的事项。

## 主题梳理

按真实讨论主题分节。每节写清楚背景、讨论内容、结论或分歧、相关数字、后续动作。没有证据的项目不要补。

## 待办

列出明确责任人、事项和时间。没有明确责任人或时间时，标注「未明确」。

## 待确认

列出会议中没有定论、需要继续确认的问题。

## 输出限制

- 不新增事实。
- 不为凑长度扩写。
- 不写高度浓缩的摘要。
- 专有名词保留原文；只有原文支持释义时，才补充释义。
- 每段只承载一个论点；超过一个论点时拆成多段。
</user_instructions>
