interface:
  display_name: "Image Generation Workbench"
  short_description: "Generate, edit, repair, and QA images with OpenAI Image 2"
  default_prompt: "Use $image-generation-workbench for source-backed image generation and editing: attach references, create or repair the visual asset, inspect the output, and keep exact text or final layout in the host document."
