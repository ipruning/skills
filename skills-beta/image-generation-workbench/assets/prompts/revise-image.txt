Revise the previous generated image while preserving the original source information.

Keep:
- the important source objects, colors, positions, and layout;
- the overall annotation and illustration style;
- existing readable labels unless the issue explicitly asks to change them.

Fix only the issue described by the user. If possible, attach the original source image again in the command so the model can re-anchor on it.

Do not invent new objects, fake text, fake numbers, fake logos, long paragraphs, watermarks, people, hands, or unrelated decorative elements.
